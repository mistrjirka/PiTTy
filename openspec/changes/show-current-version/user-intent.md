# User intent (non-normative)

- The user asked for PiTTy to visibly show its current version and authorized work directly on `main`, including pushing and releasing it.
- The user selected the release version and tag: `v0.3.1.1`.
- No location was prescribed. The accepted implementation direction is a compact, always-visible sidebar version label, using the package version so the UI and CLI help remain aligned with the release metadata.
