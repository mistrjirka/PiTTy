## Compaction card placement

- The compacted-context card (`── Compacted ──`, sizes, reason, duration, expandable summary) now **renders in the transcript flow as a regular message row**, right where the compaction notice is — not pinned below the whole conversation. It scrolls with history and stays in place when new messages arrive instead of disappearing, and older compaction notices from earlier compactions still render as plain system lines.
